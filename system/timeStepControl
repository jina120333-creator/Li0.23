// Managed by Allrun (see README_scour_comparison.md). The scour run uses
// the sedFoam 3DScour tutorial ceiling for its whole duration: the bed
// relaxation / Johnson-Jackson pff dynamics are much stiffer than the flow
// CFL, so maxDeltaT (not maxCo) is the effective limit near the bed.
maxCo           0.3;
maxAlphaCo      0.3;
maxDeltaT       2e-4;
